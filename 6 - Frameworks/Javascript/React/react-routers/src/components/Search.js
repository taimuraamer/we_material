import React from 'react';

const Search = (props) => (
    <div>
        <form>
            <input type="text" name="search" />
            <button>Search</button>
        </form>
    </div>
)

export default Search;